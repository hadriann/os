# os

A simple mock first-stage bootloader. It loads nothing, but illustrates the skeleton of a proper bootloader.

It can be compiled with nasm:
nasm bootloader.asm -o bootloader.bin -f bin

And can be tested using qemu:
qemu -fda bootloader.bin
